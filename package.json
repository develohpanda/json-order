{
  "name": "json-order",
  "version": "1.0.0",
  "description": "Control the order of properties in JSON via a lookup object",
  "main": "lib/index.js",
  "scripts": {
    "bootstrap": "flow-typed install",
    "test": "flow && jest",
    "prepack": "rimraf lib && npm run prepack:babel && npm run prepack:flow",
    "prepack:babel": "babel src/ -d lib",
    "prepack:flow": "flow-copy-source src lib"
  },
  "files": [
    "lib"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/develohpanda/json-lookup-order.git"
  },
  "keywords": [
    "JSON",
    "order"
  ],
  "author": "Opender Singh",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/develohpanda/json-lookup-order/issues"
  },
  "homepage": "https://github.com/develohpanda/json-lookup-order#readme",
  "devDependencies": {
    "@babel/plugin-transform-flow-strip-types": "^7.4.4",
    "babel-cli": "^6.26.0",
    "babel-jest": "^24.7.1",
    "babel-preset-flow": "^6.23.0",
    "flow-bin": "^0.98.0",
    "flow-copy-source": "^2.0.4",
    "flow-typed": "^2.5.1",
    "jest": "^24.7.1",
    "jshint": "^2.10.2"
  },
  "dependencies": {
    "lodash.clone": "^4.5.0",
    "lodash.clonedeep": "^4.5.0"
  }
}
